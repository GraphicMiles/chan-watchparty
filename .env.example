# ======================== CLIENT-SIDE (VITE_ prefix = bundled into browser JS) ========================
# WARNING: Never put secrets in VITE_ variables — they are visible to anyone who opens DevTools.

# Firebase client configuration (required in the browser bundle)
VITE_FIREBASE_API_KEY=replace-with-firebase-web-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=replace-with-sender-id
VITE_FIREBASE_APP_ID=replace-with-app-id
VITE_FIREBASE_MEASUREMENT_ID=

# LiveKit (public URL only in the client — secrets are server-side)
VITE_LIVEKIT_URL=wss://your-project.livekit.cloud

# Public API base URL for Capacitor/native builds. Browser web can leave this blank.
# Production API lives on Render.
VITE_API_URL=https://chan-aunk.onrender.com

# ======================== SERVER-SIDE ONLY (never use VITE_ prefix for these) ========================

# YouTube Data API v3 (server-side key — restrict to YouTube API + your Vercel origins)
YOUTUBE_API_KEY=replace-with-restricted-youtube-key

# Firebase Admin SDK — server-side only; never commit real values
FIREBASE_ADMIN_PROJECT_ID=your-project-id
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk@your-project-id.iam.gserviceaccount.com
FIREBASE_ADMIN_PRIVATE_KEY=replace-with-escaped-private-key

# LiveKit server-side credentials — never expose these in VITE_ variables
LIVEKIT_API_KEY=replace-with-livekit-api-key
LIVEKIT_API_SECRET=replace-with-livekit-api-secret

# Cron secret — REQUIRED. Used to authenticate cleanup cron requests.
CRON_SECRET=replace-with-random-cron-secret

# Optional server-side search providers
OMDB_API_KEY=replace-with-omdb-key
FOOTBALL_DATA_KEY=replace-with-football-data-key

# Groq AI (for summaries, catchup, quiz, subtitles)
GROQ_API_KEY=replace-with-groq-api-key

# O2TV episode-resolve worker. The captcha+Groq resolve can exceed Vercel
# Hobby's 10s cap, so offload it to a long-lived worker (Railway/Render/Fly).
# When O2TV_WORKER_URL is empty, the in-process resolver is used (best-effort).
# Set BOTH values on the worker (WORKER_SECRET) AND here (O2TV_WORKER_SECRET) to the
# same random string. See o2tv-worker/README.md for deployment steps.
O2TV_WORKER_URL=
O2TV_WORKER_SECRET=

# NSFW provider (disabled by default; requires compliant age/access controls)
NSFW_ENABLED=false
NSFW_PROVIDER=xvideos

# Optional explicit sports-to-IPTV mapping. Channel names are matched against the M3U catalog.
# Example: [{"competition":"Premier League","channels":["SuperSport","Sky Sports"]}]
SPORTS_CHANNEL_MAP_JSON=[]

# IPTV catalog. The default is the Free-TV playlist; override with one or more trusted HTTPS M3U URLs.
IPTV_PLAYLIST_URL=https://raw.githubusercontent.com/Free-TV/IPTV/master/playlist.m3u8
# Optional multi-provider list: [{"id":"free-tv","label":"Free-TV IPTV","url":"https://...","enabled":true}]
IPTV_PLAYLISTS_JSON=[]
# Set true after the cron has populated healthy channels in Firestore.
IPTV_USE_FIRESTORE_CATALOG=false
IPTV_HEALTH_CHECK_LIMIT=50
# Optional additional licensed/public channels, as JSON.
IPTV_CHANNELS_JSON=[]

# ======================== SECURITY ========================

# CORS: JSON array of allowed origins. If unset, all origins are allowed (backward compat).
# Example: ["https://chan-yz3p.vercel.app","https://chan.example.com"]
ALLOWED_ORIGINS=

# Proxy domain allow-list: JSON array of hostnames the /api/proxy endpoint will fetch from.
# If unset, all public domains are allowed (backward compat — set this in production!).
# Example: ["example.com","cdn.example.com","youtube.com"]
PROXY_ALLOWED_DOMAINS=

# Upstash Redis for persistent rate limiting (survives cold starts & works across instances).
# If unset, falls back to in-memory rate limiting (resets on cold starts).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
